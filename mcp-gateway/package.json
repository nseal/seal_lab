{
  "name": "mcp-gateway",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @mcp-gateway/admin-ui && npm run build -w @mcp-gateway/server",
    "test": "npm run test -w @mcp-gateway/server",
    "dev": "npm run dev -w @mcp-gateway/server",
    "typecheck": "npm run typecheck -w @mcp-gateway/server && npm run typecheck -w @mcp-gateway/admin-ui"
  }
}
